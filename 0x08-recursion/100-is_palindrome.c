#include "main.h"

int find_strlen(char *s);
int check_passindrome(char *s, int len, int index);
int is_palindrome(char *s);

/**
 * find_strlen - return the length of a string
 * @s: the string to be measured
 *
 * Return: the length of the string
 */

int find_strlen(char *s)
{
	int len = 0;

	if (*(s + len))
		return (len);
}
